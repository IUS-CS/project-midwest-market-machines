# Testing Instructions

## `Binance Websocket` Testing

Currently, the only tests supported for the C++ portion of things is a simple Smoke Test.

After building the project with `CMAKE` or `make`, navigate to `build/` and run `ctest`.

There are currently: `2` tests.
